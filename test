




pthread_lock(left_fork);
printf("has taken a fork")
pthread_unlock(left_fork);



memset(&fork, 0, sizeof(char *));

pthread_lock(left_fork);
if (fork[0] = 0)
{
	fork[0] = 1
	printf("has taken a fork")
}
else
	;
pthread_unlock(left_fork);
